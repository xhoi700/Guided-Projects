#include <stdio.h>

int main()
{
    int i = 16;

    if (i % 2 == 0)
    {
        printf("the number is even %d", i);
    }
    else
    {
        printf("the number is odd %d", i);
    }
     return (0);
}
   
