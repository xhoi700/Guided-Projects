#include "bank.h"
#include <stdio.h>

void viewBalance(BankClient clients[], int size, int accountNumber)
{
    int i;

    for (i = 0; i < size; i++)
    {
        printf("Account balance is %s\n", )
    }
}